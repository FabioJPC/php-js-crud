services:
  frontend:
    container_name: frontend
    build:
      context: ./frontend
      dockerfile: Dockerfile
    ports: 
      - "5000:80"

  backend:
    container_name: backend
    build: 
      context: ./crud-api
      dockerfile: Dockerfile
    ports:
    - "8080:80"